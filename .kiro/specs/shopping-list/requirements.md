# Requirements Document

## Project Description (Input)
残量 `low` / `empty` のアイテムを戻し場所付きで一覧化した buy 候補リスト（FR-05）の生成と表示（I-04 画面）。単独 Provider では生成不能な横断的示唆として、複数 Provider の統合ビューから残量状態でフィルタリングし、収納場所情報と合わせて買い物リストを構築する。

## Requirements
<!-- Will be generated in /kiro:spec-requirements phase -->
