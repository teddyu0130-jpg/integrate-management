# Requirements Document

## Project Description (Input)
統合層でのみ検出可能な重複登録（`name` 一致 & `iri` 不一致）を検知し、ユーザーに通知する機能（FR-11）。各 Provider は自身のデータしか持たないため単独では重複を検出できず、App③ の統合ビュー上で初めて名前衝突が発見できる。検出した重複候補の表示・マージ誘導 UI を提供する。

## Requirements
<!-- Will be generated in /kiro:spec-requirements phase -->
