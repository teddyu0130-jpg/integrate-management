# Requirements Document

## Project Description (Input)
`ontologies/*.yaml` による Provider ごとの属性マッピング定義。`app1_mapping.yaml`・`app2_mapping.yaml`・`unified_view.yaml` の設計と、新 Provider 追加や書き込み先変更をコード変更なしに辞書追記だけで対応できる仕組みの設計・実装。TypeScript 内に Provider フィールド名をハードコードしない黄金律を保証する構造。

## Requirements
<!-- Will be generated in /kiro:spec-requirements phase -->
